<?xml version="1.0" encoding="utf-8"?>
<!--
    Activity Read Fragment Layout: Displays an empty state message for read notifications.
    Optimized: Localized strings and clean FrameLayout.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/layout_activity_read_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/back_primary_color">

    <TextView
        android:id="@+id/tv_empty_read_msg"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="@string/activity_empty_read"
        android:textColor="@color/black"
        android:alpha="0.4"
        android:textSize="16sp" />

</FrameLayout>
